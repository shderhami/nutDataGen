requests>=2.28.0
python-dotenv>=1.0.0
# >=0.79.0: AI validation passes output_config.format (structured outputs);
# older SDKs reject the parameter.
anthropic>=0.79.0
psycopg2-binary>=2.9.9
alembic>=1.13.0
sqlalchemy>=2.0.0
pytest>=7.0.0
pytest-cov>=4.0.0
